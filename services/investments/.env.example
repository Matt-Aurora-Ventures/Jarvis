# ============================================================
# Jarvis Investment Service — Environment Variables
# ============================================================
# Copy to .env and fill in values before running.
# Only the LLM keys are required for dry-run mode.
# ============================================================

# --- Mode ---
DRY_RUN=true                    # true = mock basket, no real trades
BASKET_ID=alpha                 # Basket identifier in database
INVESTMENT_API_PORT=8770        # FastAPI port

# --- LLM API Keys (required) ---
XAI_API_KEY=                    # Grok/xAI for sentiment + trader
ANTHROPIC_API_KEY=              # Claude for risk analysis + debate
OPENAI_API_KEY=                 # ChatGPT for macro analysis + debate

# --- Database ---
DATABASE_URL=postgresql://jarvis:jarvis_dev@localhost:5433/jarvis_investments
REDIS_URL=redis://localhost:6380

# --- Price Feeds ---
BIRDEYE_API_KEY=                # BirdEye API for Dexter fundamentals

# --- Base Chain (EVM) — required when DRY_RUN=false ---
BASE_RPC_URL=https://mainnet.base.org
MANAGEMENT_WALLET_KEY=          # Private key for rebalance transactions
BASKET_ADDRESS=                 # ERC-7621 basket contract address

# --- Solana — required for bridge + staking ---
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
SOLANA_WALLET_KEY=              # Base58 private key for staking cranker
STAKING_POOL_ADDRESS=           # Anchor staking pool PDA
AUTHORITY_REWARD_ACCOUNT=       # USDC account for reward deposits

# --- API Authentication ---
INVESTMENT_ADMIN_KEY=           # Bearer token for write endpoints (kill switch, trigger)
