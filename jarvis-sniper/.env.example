# ─────────────────────────────────────────────────────────
# Jarvis Sniper — Environment Variables
# Copy to .env.local and fill in values before deploying.
# ─────────────────────────────────────────────────────────

# ── Solana RPC (REQUIRED) ──────────────────────────────
# Use a provider (Helius/QuickNode/etc) for production.
# IMPORTANT:
# - `SOLANA_RPC_URL` is server-only (safe for API keys).
# - `NEXT_PUBLIC_SOLANA_RPC` is client-exposed (use a public endpoint only).
SOLANA_RPC_URL=
NEXT_PUBLIC_SOLANA_RPC=https://api.mainnet-beta.solana.com

# ── Bags.fm (REQUIRED for trading) ─────────────────────
# Server-side API key used by /api/bags/* proxy routes.
BAGS_API_KEY=

# Bags referral account (optional, earns referral fees)
BAGS_REFERRAL_ACCOUNT=

# ── Helius (optional) ──────────────────────────────────
# Used by the backtest scorer for historical data.
HELIUS_API_KEY=

# ── Production Hosting ───────────────────────────────
# Comma-separated list of allowed origins for CORS.
# Required when hosting for multiple users.
# Example: https://sniper.example.com,https://app.example.com
ALLOWED_ORIGINS=

# Port to run on (default: 3001)
PORT=3001

# Node environment
NODE_ENV=production
