# JARVIS Web Demo - Backend Dependencies

# Core Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Security
python-jose[cryptography]>=3.5.0  # FIXED: CVE-2024-33663 algorithm confusion with ECDSA keys (GitHub Dependabot #28)
passlib[bcrypt]==1.7.4
bcrypt==4.1.2
python-multipart>=0.0.20  # FIXED: DoS via boundary deformation #25, arbitrary file write #39
slowapi==0.1.9  # Rate limiting

# Database
sqlalchemy==2.0.25
asyncpg==0.29.0  # PostgreSQL async driver
alembic==1.13.1  # Migrations
redis==5.0.1  # Cache and sessions

# HTTP & Networking
aiohttp>=3.11.18  # FIXED: zip bomb #31, large payloads #36, bypassing asserts #35, chunked msgs #37
httpx==0.26.0
websockets==12.0

# Solana & Trading
solana==0.30.2
solders==0.18.0  # Solana SDK
base58==2.1.1

# AI & Sentiment (supports both Grok and Ollama)
openai==1.10.0  # For OpenAI-compatible APIs (Grok, Ollama)

# Charts & Visualization
matplotlib==3.8.2
pillow>=10.4.0  # FIXED: Buffer overflow vulnerability (GitHub Dependabot #20)

# Utilities
python-dotenv==1.0.0
cryptography>=44.0.2  # FIXED: NULL pointer dereference (GitHub Dependabot #18)
argon2-cffi==23.1.0
tenacity==8.2.3  # Retry logic

# Monitoring & Logging
prometheus-client==0.19.0
sentry-sdk[fastapi]==1.40.0

# Development
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
httpx==0.26.0  # For testing API
faker==22.5.0  # Test data generation
black==24.3.0  # Code formatting
ruff==0.1.11  # Linting
mypy==1.8.0  # Type checking
