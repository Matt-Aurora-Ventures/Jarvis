# Treasury Weekly Report

**Report Period:** {{ start_date }} - {{ end_date }}
**Generated:** {{ generated_at }}

---

## Executive Summary

| Metric | Value | Change |
|--------|-------|--------|
| Total Treasury Value | ${{ total_value | format_usd }} | {{ total_change | format_percent }} |
| Weekly P&L | ${{ weekly_pnl | format_usd }} | {{ pnl_change | format_percent }} |
| Total Trades | {{ trade_count }} | {{ trade_change | format_change }} |
| Win Rate | {{ win_rate | format_percent }} | {{ winrate_change | format_percent }} |

---

## Wallet Balances

| Wallet | Type | Balance | USD Value |
|--------|------|---------|-----------|
{% for wallet in wallets %}
| {{ wallet.name }} | {{ wallet.type }} | {{ wallet.balance | format_amount }} {{ wallet.token }} | ${{ wallet.usd_value | format_usd }} |
{% endfor %}

**Total:** ${{ total_balance | format_usd }}

---

## Trading Performance

### Strategy Breakdown

| Strategy | Trades | Win Rate | P&L | Sharpe |
|----------|--------|----------|-----|--------|
{% for strategy in strategies %}
| {{ strategy.name }} | {{ strategy.trades }} | {{ strategy.win_rate | format_percent }} | ${{ strategy.pnl | format_usd }} | {{ strategy.sharpe | format_number(2) }} |
{% endfor %}

### Top Performing Trades

| Token | Strategy | Entry | Exit | P&L | Return |
|-------|----------|-------|------|-----|--------|
{% for trade in top_trades %}
| {{ trade.token }} | {{ trade.strategy }} | ${{ trade.entry | format_usd }} | ${{ trade.exit | format_usd }} | ${{ trade.pnl | format_usd }} | {{ trade.return | format_percent }} |
{% endfor %}

### Worst Performing Trades

| Token | Strategy | Entry | Exit | P&L | Return |
|-------|----------|-------|------|-----|--------|
{% for trade in worst_trades %}
| {{ trade.token }} | {{ trade.strategy }} | ${{ trade.entry | format_usd }} | ${{ trade.exit | format_usd }} | ${{ trade.pnl | format_usd }} | {{ trade.return | format_percent }} |
{% endfor %}

---

## Distributions

| Type | Amount | Recipients | Date |
|------|--------|------------|------|
{% for dist in distributions %}
| {{ dist.type }} | {{ dist.amount | format_amount }} {{ dist.token }} | {{ dist.recipients | format_number }} | {{ dist.date }} |
{% endfor %}

**Total Distributed:** {{ total_distributed | format_amount }} tokens

---

## Risk Metrics

| Metric | Value | Status |
|--------|-------|--------|
| Max Drawdown | {{ max_drawdown | format_percent }} | {{ drawdown_status }} |
| Sharpe Ratio | {{ sharpe_ratio | format_number(2) }} | {{ sharpe_status }} |
| Sortino Ratio | {{ sortino_ratio | format_number(2) }} | {{ sortino_status }} |
| Daily VaR (95%) | ${{ var_95 | format_usd }} | {{ var_status }} |
| Exposure % | {{ exposure_percent | format_percent }} | {{ exposure_status }} |

---

## Notes

{{ notes }}

---

*This report was automatically generated by JARVIS Treasury Management System.*
*For questions, contact the treasury team.*
