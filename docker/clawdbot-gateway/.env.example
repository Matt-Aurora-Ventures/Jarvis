# ============================================================================
# ClawdBot Gateway - Environment Variables Template
# ============================================================================
# SECURITY: Copy this to .env and fill in real values
# NEVER commit .env to git - only this .env.example is safe to commit
# ============================================================================

# --- Telegram Bot Tokens ---
# Create bots via @BotFather on Telegram
FRIDAY_TELEGRAM_TOKEN=your_friday_bot_token_here
MATT_TELEGRAM_TOKEN=your_matt_bot_token_here
JARVIS_TELEGRAM_TOKEN=your_jarvis_bot_token_here

# --- Gateway Auth Tokens (unique per agent) ---
# Generate new ones with: openssl rand -hex 32
FRIDAY_AUTH_TOKEN=generate_with_openssl_rand_hex_32
MATT_AUTH_TOKEN=generate_with_openssl_rand_hex_32
JARVIS_AUTH_TOKEN=generate_with_openssl_rand_hex_32

# --- Supermemory (Shared Memory Backend) ---
# Get API key from https://console.supermemory.ai
# No OpenAI key needed - Supermemory handles embeddings
SUPERMEMORY_API_KEY=sm_your_supermemory_api_key_here

# --- LLM API Keys ---
# Anthropic OAuth (for Claude models)
ANTHROPIC_OAUTH_ACCESS=sk-ant-oat01-your_access_token
ANTHROPIC_OAUTH_REFRESH=sk-ant-ort01-your_refresh_token

# XAI/Grok (for Jarvis CTO role)
XAI_API_KEY=xai-your_xai_api_key

# Google AI (Gemini)
GOOGLE_AI_KEY=AIza-your_google_ai_key

# --- Optional API Keys ---
# Only needed if using these features
OPENAI_API_KEY=sk-your_openai_key_if_needed
SAG_API_KEY=sk_your_sag_key_if_needed
TWITTER_API_KEY=your_twitter_api_key_if_needed

# --- Admin Notifications ---
# Telegram chat ID for watchdog alerts
TELEGRAM_ADMIN_CHAT_ID=your_telegram_user_id
