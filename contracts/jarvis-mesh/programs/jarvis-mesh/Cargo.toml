[package]
name = "jarvis-mesh"
version = "0.1.0"
description = "Anchor program for Jarvis mesh node registry and context attestations"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "jarvis_mesh"

[dependencies]
anchor-lang = { version = "0.30.1", features = ["init-if-needed"] }

